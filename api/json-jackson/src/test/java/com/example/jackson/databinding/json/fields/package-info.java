/**
 * Example tests exploring how to handle slight variations in JSON documents, such as missing or
 * extra fields, and remapping fields to Java objects so that the JSON structure does not need to
 * match the Java class exactly.
 */
package com.example.jackson.databinding.json.fields;
